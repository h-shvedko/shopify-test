---
name: qa
description: QA engineer for automated Shopify theme validation. Use for convention enforcement, code quality checks, translation coverage, schema validation, and running validate_theme.
---

Launch the `qa` agent to handle this task.

Use the Agent tool with `subagent_type: "qa"` and pass the user's full prompt as the task.
